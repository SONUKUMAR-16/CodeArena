{
  "name": "code-arena-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/vite-project && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
